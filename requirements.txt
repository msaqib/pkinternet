# pkinternet — Python dependencies
#
# Third-party packages imported anywhere in this repository. Pinned loosely on
# purpose: the analysis is not sensitive to minor versions, and over-pinning ages
# badly. Python 3.10+ (the analysis scripts use match statements and | type unions).
#
#   python -m venv .venv && . .venv/bin/activate   # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# NOTE: pybgpstream needs the libbgpstream C library installed first and will NOT
# build from pip alone. It is used only by the BGP experiments (05, 09); everything
# else runs without it. See https://bgpstream.caida.org/docs/install

# --- measurement + data ---
requests>=2.31            # RIPE Atlas + RIPEstat REST calls
ripe.atlas.cousteau>=2.0  # RIPE Atlas measurement creation / result fetching
ripe.atlas.sagan>=1.3     # parsing Atlas traceroute/ping/DNS results
dnspython>=2.4            # per-ISP DNS resolution (Exp 01.1)
python-dotenv>=1.0        # ATLAS_API_KEY out of the source tree

# --- analysis ---
pandas>=2.0
numpy>=1.24
scipy>=1.10                # non-parametric tests (Mann-Whitney, bootstrap CIs)

# --- figures ---
matplotlib>=3.7
seaborn>=0.12
folium>=0.14               # probe maps
shapely>=2.0               # geodesic / great-circle work

# --- target sampling ---
tranco>=0.8                # reproducible top-sites list (Exp 01.4 PK100)

# --- optional: the small Flask dashboard in tools/ ---
flask>=3.0
waitress>=2.1

# --- optional: BGP experiments only (needs libbgpstream, see note above) ---
# pybgpstream>=2.0
